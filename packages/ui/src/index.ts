/**
 * Shared design-system components (Tailwind + shadcn/ui style) consumed by
 * apps/web. The dashboard widget registry maps a manifest's `component` id
 * (e.g. 'inventory.LowStock') to a React component exported from here.
 *
 * TODO(Phase 1): add primitives (Button, Card, Table, Chart) + the widget map.
 */
export const UI_PACKAGE = '@aion/ui';
